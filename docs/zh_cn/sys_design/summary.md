# conf-agent 系统设计文档索引

本目录存放 `conf-agent` 的系统级设计文档，覆盖总体架构、模块组织、启动流程以及配置加载与热加载等关键流程的细节设计。

---

## 一、总体与分层设计

| 文档名称 | 相对路径 | 摘要说明 |
|---------|---------|---------|
| 总体设计文档 | [总体设计文档.md](./总体设计文档.md) | 描述 `conf-agent` 在 AI 网关中的定位、功能范围、核心模块、启动流程、配置模型以及与 `ai-gateway-api` 和 `BFE` 的交互关系。 |

---

## 二、细节设计（details/）

| 文档名称 | 相对路径 | 摘要说明 |
|---------|---------|---------|
| 配置加载与热加载流程 | [details/配置加载与热加载流程.md](./details/配置加载与热加载流程.md) | 描述 `conf-agent` 从 `ai-gateway-api` 拉取配置、写入临时目录、触发 `BFE` 热加载、切换符号链接的完整流程，以及配置目录版本管理策略。 |

---

## 三、阅读建议

1. **快速建立全局认知**：先阅读《总体设计文档.md》。
2. **理解配置加载流程**：阅读《details/配置加载与热加载流程.md》。
3. **查看配置项说明**：参考《../config/config.md》。
